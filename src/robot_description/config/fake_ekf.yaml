ekf_filter_node:
  ros__parameters:

    frequency: 30.0
    sensor_timeout: 0.1
    two_d_mode: true

    publish_tf: true

    base_link_frame: base_link
    odom_frame: odom
    world_frame: odom

    odom0: /cmd_vel
    odom0_config: [false, false, false,
                   false, false, true,
                   true, true, false,
                   false, false, false,
                   false, false, false]

    use_sim_time: false